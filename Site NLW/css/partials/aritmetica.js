var numeros = [];
var valorMedia = Number(prompt("Digite valor da media"))

for (i = 0; i < valorMedia.lenght; i++){
    numeros[i] = prompt("Digite os valores")
    var result =+ numeros[i];
}
console.log(result)
